/// Constants used across domain entities
class DomainConstants {
  /// Default currency for all financial entities
  static const String defaultCurrency = 'MYR';

  /// Default theme for user settings
  static const String defaultTheme = 'light';

  /// Default epsilon for double comparisons
  static const double epsilon = 0.001;
}
